hud:
  enabled: true
  panel_alpha: 0.55
  crosshair:
    enabled: true
  compass:
    enabled: true
  depth_alt:
    enabled: true
  # Optional status chips on the video feed (mode / ESP32 link / E-STOP).
  # Powered by live system state from the mission computer, never fake data.
  extra:
    enabled: true
  colors:
    primary: [100, 255, 218]
    secondary: [173, 199, 255]
    on_surface: [214, 227, 255]
    on_surface_variant: [186, 202, 195]
    bg: [17, 34, 64]
    bg_high: [39, 53, 76]
